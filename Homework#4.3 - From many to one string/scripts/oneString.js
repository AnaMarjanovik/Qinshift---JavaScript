function final(array){
    let add = '';
    for(let i=0; i<array.length; i++){
        add = add + ' ' + array[i] + '';
    }
    console.log(add);
}

final(['Hello','from', 'Qinshift', 'Academy','!']);
